package add_digits;

import helper.BasicFunctions;

public class Runner {
    public static void main(String[] args) {
        BasicFunctions.print(new Solution().addDigits(38));
        BasicFunctions.print(new Solution2().addDigits(45));
    }
}
